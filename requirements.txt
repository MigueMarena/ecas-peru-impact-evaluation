# Dependencias Python del paquete de replicación.
#
# Solo cubren el código publicado en code/. Las versiones son mínimos probados,
# no pines exactos: ninguno de los cuatro scripts depende de APIs recientes.
#
#     pip install -r requirements.txt
#
# El grueso del pipeline es Stata; Python interviene en tres puntos: el diagrama
# CONSORT, la validación del .docx del reporte y el mapa de tablas.

# Diagrama CONSORT — code/I_diagnostics/I2_graph_consort.py
matplotlib>=3.7
pandas>=2.0
openpyxl>=3.1          # backend que pandas necesita para leer el .xlsx de I1
numpy>=1.24            # llega como dependencia de pandas/matplotlib

# Validación del paquete .docx — code/_utils/check_sections.py
lxml>=4.9

# code/_utils/build_table_map.py y fix_compiled_docx.py usan solo la
# biblioteca estándar (csv, re, zipfile, shutil, pathlib): no requieren nada.
